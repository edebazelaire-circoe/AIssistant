# Secrets are referenced by name from the application settings and never persisted.
OPENAI_API_KEY=
# Optional overrides. The UI remains the source of truth for non-secret settings.
JARVIS_DATA_DIR=./data
JARVIS_ALLOWED_ROOT=./workspace
JARVIS_HOST=127.0.0.1
JARVIS_PORT=8765
